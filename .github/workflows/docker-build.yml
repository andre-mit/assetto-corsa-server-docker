name: Build and Push AC Server Image

on:
  push:
    branches:
      - main
    paths:
      - 'ac-server/Dockerfile'
      - 'ac-server/entrypoint.sh'
      - '.github/workflows/docker-build.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }} 

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-server

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./ac-server/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-server:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-server:${{ github.sha }}
          labels: ${{ steps.meta.outputs.labels }}
  trigger-deploy:
    name: Trigger Coolify
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Call Coolify Webhook
        run: |
          curl -f -v --request GET '${{ secrets.COOLIFY_URL_DEPLOY }}${{ secrets.COOLIFY_AC_SERVER_ID }}&force=true&sha=${{ github.sha }}' \
              --header 'Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}'